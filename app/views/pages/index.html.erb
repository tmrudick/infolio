<div id="navbar">
		<div id="logo">
		</div>
	</div>
	<div id="wrapper">
		<div id="profile">
			<div id="content">
				<h1><%= current_user.first_name %></h1>
				<p><%= Twitter.user("#{current_user.twitter}").description %></p>
				<br>
				
				<div id="socialicons">
				<%= image_tag("fblogo.png", :size => "25x25") %>
				<%= if current_user.twitter 
				image_tag("twitterlogo.png", :size => "25x25") 
				end %>
				</div>
			</div>
		</div>
		<div id="avatar">
		<%= image_tag "/data/#{current_user.id}/profilePic", :class => "profilePic"%>
		</div>
		<div id="module1">
			<div class="block1">
				<p>Friend Network</p>
				<hr>
			</div>
			<div class="block1">
				<p>Type of Check-Ins</p>
				<hr>
        <canvas id="donut" datasrc="/assets/donut.pjs" width="200" height="200"></canvas>
			</div>
			<div class="block1">
				<p>Type of Likes</p>
				<hr>
			</div>
		</div>
		<div id="module2">
			<div class="block2">
				<p>Twitter Activity</p>
				<hr>
				<p>
				<%= 
				Twitter.search("from:#{current_user.twitter}", :include_entities=>true, :rpp => 5, :result_type => "recent").map do |status|
					"#{status.text}" 
 				  end %>
				</p>
			</div>
		</div>
		<div id="module3">
			<div class="block3">
				<p class="block3p">Twitter Statistics</p>
				<hr>
				<p class="statscaption force">Last seen in</p>
				<p class="statsdata force"><%= Twitter.user("#{current_user.twitter}").location %></p>
				<p class="statscaption force">Number of tweets</p>
				<p class="statsdata force"><%= Twitter.user("#{current_user.twitter}").statuses_count %></p>
				<p class="statscaption force">Number of followers</p>
				<p class="statsdata force"><%= Twitter.user("#{current_user.twitter}").followers_count %></p>
			</div>
		</div>
		<div id="module4">
		</div>
	</div>
	
<script type="text/javascript">
  var postData;
  var getPosts = $.get("/data/<%= current_user.id %>/posts", function(results) {
    postData = results;
  });

  var placesData;
  var getPlaces = $.get("/data/<%= current_user.id %>/places", function(results) {
    placeData = results;
  });

  $(function() {
    $.when(getPosts, getPlaces).then(function() {
      // do something with data
    });
  });
</script>
